rules_version = '2';
// Deploy to the dedicated prescription bucket only. Access goes through the authenticated server.
service firebase.storage {
  match /b/{bucket}/o {
    match /{object=**} { allow read, write: if false; }
  }
}
